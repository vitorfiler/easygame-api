server:
  port: 1002

spring:
  datasource:
    url: jdbc:h2:mem:core
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
        